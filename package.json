{
    "name": "simple-steps",
    "version": "0.0.0",
    "author": "ZSkycat",
    "repository": {
        "type": "git",
        "url": "https://github.com/ZSkycat/simple-steps"
    },
    "license": "MIT",
    "description": "A module that facilitates the display of step status and time in the terminal.",
    "keywords": [
        "simple",
        "step",
        "log"
    ],
    "main": "./dist/index.js",
    "files": [
        "dist/",
        "src/"
    ],
    "scripts": {
        "clear": "rimraf ./dist/**/*",
        "test": "mocha --require ts-node/register ./test/**/*",
        "build": "npm run clear && tsc -p ."
    },
    "dependencies": {
        "ansi-regex": "^3.0.0",
        "chalk": "^2.0.0",
        "cjk-regex": "^2.0.0"
    },
    "devDependencies": {
        "@types/mocha": "^5.2.4",
        "@types/node": "^10.5.2",
        "mocha": "^5.2.0",
        "rimraf": "^2.6.2",
        "ts-node": "^7.0.0",
        "tsc": "^1.20150623.0",
        "typescript": "^2.9.2"
    }
}
